const { expect } = require("chai");
const { uniquePaths } = require("../uniquePaths/uniquePaths");

describe("uniquePaths", () => {
  it("should exist", () => {
    expect(uniquePaths).not.to.be.undefined;
  });
});
